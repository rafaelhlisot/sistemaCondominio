import styled from 'styled-components/native';

export default {
    container: styled.SafeAreaView`
        flex: 1;
        justify-content: center;
        align-items: center;
    `,

    Texto: styled.Text`
    
    `
};